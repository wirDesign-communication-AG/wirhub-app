name: Docker Test Image

on:
    schedule:
        -   cron: '0 9 * * 5'
    push:
        branches: [ 'main', 'v3.2', 'v3.2.*' ]
        paths:
            - build/docker/test/Dockerfile

jobs:
    test:
        runs-on: ubuntu-latest
        permissions:
            contents: read
            packages: write
            id-token: write

        steps:
            -   name: Checkout repository
                uses: actions/checkout@v6

            -   name: Setup Docker buildx
                uses: docker/setup-buildx-action@v4

            -   name: Log into ghcr.io
                if: github.event_name != 'pull_request'
                uses: docker/login-action@v4
                with:
                    registry: ghcr.io
                    username: ${{ github.actor }}
                    password: ${{ secrets.GITHUB_TOKEN }}

            -   name: Build & Push
                uses: docker/build-push-action@v7
                with:
                    context: .
                    file: build/docker/test/Dockerfile
                    push: true
                    tags: |
                        ghcr.io/wirdesign-communication-ag/wirhub-app/test:latest
                    provenance: false
                    cache-from: type=registry,ref=ghcr.io/wirdesign-communication-ag/wirhub-app/test:buildcache
                    cache-to: type=registry,ref=ghcr.io/wirdesign-communication-ag/wirhub-app/test:buildcache,mode=max,image-manifest=true,oci-mediatypes=true,ignore-error=true
